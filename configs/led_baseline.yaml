# LED Baseline Configuration

model_name: led
run_name: led_baseline

data_dir: data/processed
max_input_length: 4096
max_target_length: 128

batch_size: 2
learning_rate: 3e-5
num_epochs: 3
warmup_steps: 500
max_grad_norm: 1.0
seed: 42
